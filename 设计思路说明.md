# NetDesigning 建筑设计展示网站 — 设计思路与架构

> 项目路径：`C:\Users\Administrator\Desktop\NetDesigning\`

---

## 一、核心理念：让建筑方案自己说话

建筑设计作品的展示不同于普通网页——它需要兼顾**视觉冲击力**、**信息层级清晰**和**浏览的沉浸感**。本项目的核心目标不是做一个"好看"的网页，而是建立一个**建筑方案的数字化展陈系统**，让每一张图纸、每一段说明都能以最恰当的方式呈现。

为此，系统围绕三个原则展开：

| 原则 | 说明 |
|------|------|
| **内容优先** | 所有设计围绕如何更好地呈现建筑图纸和说明展开 |
| **灵活编排** | 不同方案需要不同的版式，用结构化编辑而非硬编码 |
| **沉浸底色** | 用克莱因蓝统一视觉基调，让图像与背景自然融合 |

---

## 二、呈现结构的层级设计

系统采用 **四级层级结构** 来组织内容，兼顾宏观浏览与微观编辑：

```
页面 (Page)          ← 项目 / 方案 / 章节
  └── 区块 (Block)   ← 功能模块 / 主题分区
       └── 行 (Row)   ← 内容条 / 对比组 (每一行 = PPT 一页)
            └── 单元格 (Cell)  ← 最小内容单元 (文本 / 图片)
```

### 各层级说明

**页面**：对应一个完整的建筑方案或展陈章节。页面之间通过侧边栏导航切换，支持 01/02/03 编号系统，在展示网站中通过大幅页码增强视觉节奏。

**区块**：页面内的功能分区，每个区块有独立的标题标签。区块可以拖拽排序，调整呈现顺序。典型的区块划分方式：设计说明区块、图纸区块、分析图区块、技术构造区块等。

**行**：区块内的横向内容条 — 这是系统的核心呈现单元。每一行可以包含 1-4 个单元格，支持不同版式。行的高度支持拖动调整（100px-800px）。行是 PPT 导出的基本单位（每行 = 一张幻灯片）。

**单元格**：最小的内容单元，支持三种模式：

| 类型 | 说明 | 呈现方式 |
|------|------|---------|
| **文本** | 说明文字 | 支持换行分段，三种字体级别（标题/正文/注释），RGB 背景色 |
| **图片** | 设计图纸/渲染图 | 等比显示 + 图片说明文字 |
| **卷轴** | 超大/长图 | 拖拽平移 + 缩放查看（适合大尺寸总平面图、构造详图） |

---

## 三、多样化的呈现方式

为了适应建筑设计中不同类型的展示需求，系统提供了以下呈现手段：

### 3.1 灵活的列数控制

每一行可独立设置 **1-4 列**，适应不同内容：

```
1列：大图展示 / 整段说明               [████████]
2列：图+文对比 / 方案对比              [████][████]
3列：分析图并排 / 构造节点展示         [███][███][███]
4列：细节对比 / 材料样板               [██][██][██][██]
```

### 3.2 卷轴模式（超大图纸）

针对建筑设计中最常见的超大图纸（总平面图、构造详图、大样图等），系统提供了**卷轴模式**：
- 图片填满容器，保持原始比例
- 鼠标拖拽平移查看任意区域
- 滚轮缩放（0.5x - 3x）
- 一键复位按钮

这一模式模拟了在桌面上展开纸质图纸的浏览体验，避免了大图被强行缩放到看不清细节的问题。

### 3.3 三级别字体系统

为了统一管理所有文本的视觉风格，设定了三个字体级别：

| 级别 | 默认字体 | 默认大小 | 默认颜色 | 用途 |
|------|---------|---------|---------|------|
| 标题 (Title) | 微软雅黑 | 32px | #ffffff | 页面名称、大标题 |
| 正文 (Content) | 微软雅黑 | 16px | #ffffff | 方案说明、设计理念 |
| 注释 (Note) | 微软雅黑 | 14px | #cccccc | 图片说明、副标题、页码 |

全部通过 CSS 变量实现，编辑器中可一站式调整字体、字号、颜色。

### 3.4 全局与局部背景色

除了默认的克莱因蓝（#130a91），每行支持独立的 RGB 背景色调整，用于：
- 区分不同功能区域
- 配合图片色调创建视觉过渡
- 在关键部分创造色彩重点

---

## 四、克莱因蓝与"背景消融"技术

### 4.1 为什么是克莱因蓝？

克莱因蓝（International Klein Blue, IKB）是一种极致纯粹的深蓝色，接近 #130a91。它在建筑展示中有独特的优势：
- **深邃感**：深色背景让建筑图纸的白色线条和亮色区域更加突出
- **专业感**：深蓝色传达沉稳、专业的视觉气质，适合学术/设计场景
- **包容性**：深色背景可以"消化"各种尺寸和色调的图片，无需统一裁剪

### 4.2 背景消融（Background Melting）的实现

建筑设计中大量图纸和照片带有**深色或黑色背景**——如渲染图的黑背景、分析图的深色底、剖面图的暗色区域。在克莱因蓝的整体背景上，这些图片的深色边缘会自然"消融"在页面背景中，形成**无缝融合**的视觉效果。

具体实现方式：

```
图片的深色背景 ──→ 在 #130a91 背景上 ──→ 视觉上"消失"
        │                                        │
        ▼                                        ▼
   [原始图片]          [嵌入页面后]          [融为一体]
   ┌────────┐         ┌────────────────┐
   │ 建筑   │         │  ~~~~~~~~~~~~   │
   │ 图纸   │   →     │ ┌──────────┐   │  ← 图片看起来像是
   │ (黑底) │         │ │  建筑图纸  │   │     页面的一部分
   └────────┘         │ └──────────┘   │
                      │  ~~~~~~~~~~~~   │
  深色边缘明显        └────────────────┘
                      深色边缘消失在背景中
```

**技术关键**：图片容器默认不设背景色（透明），让图片自身的深色区域与页面背景 #130a91 直接叠加。当图片边缘是深色/黑色时，人眼无法区分图片边缘和页面背景，产生"漂浮"的视觉效果。

对比效果：

| 配置 | 效果 |
|------|------|
| 图片在白底上 | 深色边缘清晰可见，图片被"框住" |
| 图片在 #130a91 上 | 深色边缘融入背景，图片仿佛悬浮在页面上 |
| 配合行背景色调整 | 可以进一步消融图片特定色调区域 |

---

## 五、编辑器架构：可视化编辑而非写代码

这是本系统最重要的设计决策——**创建一个可视化编辑器，让内容管理和版面设计通过点击和拖拽完成，而非直接编写 HTML/CSS/JavaScript。**

### 5.1 为什么不做静态页面？

建筑方案的展陈需要频繁调整：
- 方案在不断迭代，图纸和说明持续更新
- 不同评委/观众关注点不同，内容顺序需要灵活调整
- 同一套图纸可能有不同的叙事逻辑

静态页面每次修改都要改代码，效率极低且容易出错。

### 5.2 双网站架构

```
┌─────────────────┐         ┌─────────────────┐
│    编辑器        │         │    展示网站      │
│  editor/        │  ────→  │  display/       │
│                 │  数据    │                 │
│  · 所见即所得    │  同步    │  · 只读展示      │
│  · 页面/区块管理  │         │  · 侧边栏导航    │
│  · 拖拽排序      │         │  · 卷轴交互      │
│  · 全局设置      │         │  · 动画过渡      │
└─────────────────┘         └─────────────────┘
        │                          │
        └──────────┬───────────────┘
                   ▼
          localStorage (siteData)
                   │
                   ▼
          export-ppt.js → .pptx
```

**编辑器**负责内容管理和版面编排，提供完整的可视化操作界面。

**展示网站**负责最终呈现，只读模式，确保浏览体验的纯粹性。

两者通过 `localStorage` 共享数据——在编辑器中点击"同步展示"后，打开展示网站即可看到最新内容。

### 5.3 编辑器功能清单

| 功能 | 操作方式 |
|------|---------|
| 页面管理 | 新增/删除/重命名，树形导航 |
| 区块管理 | 新增/删除/重命名，拖拽排序，↑↓移动 |
| 行管理 | 新增/删除，↑↓移动，高度拖动调整 |
| 单元格 | 新增/删除，图文模式切换 |
| 图片管理 | 文件选择器，自动存入 image/ 文件夹 |
| 卷轴模式 | 单元格级切换 |
| 全局设置 | 弹窗界面（RGB背景色 + 三级字体设置） |
| 数据导出 | JSON 导出 / PPT 导出 |

### 5.4 数据流

```
用户操作 → 更新 siteData (JS对象)
                ↓
          localStorage.setItem()
                ↓
         saveData() → 持久化
                ↓
   renderTree() + renderPageContent() → 界面刷新
                ↓
   syncToDisplay() → 展示网站读取最新数据
```

所有操作最终都反映在 `siteData` 这个 JSON 结构上，因此数据可导出、可迁移、可备份。

---

## 六、技术实现要点

### 6.1 技术栈

| 层 | 技术 |
|----|------|
| 前端框架 | 原生 HTML + CSS + JavaScript（零依赖） |
| 数据存储 | localStorage（浏览器本地） |
| 图片存储 | 文件系统 `image/` 文件夹 |
| PPT 导出 | Node.js + pptxgenjs |
| 字体 | 系统字体（微软雅黑/黑体/宋体/仿宋/楷体） |
| 颜色系统 | CSS 变量 + HEX/RGB 双模式 |

### 6.2 文件结构

```
NetDesigning/
├── editor/
│   ├── index.html      # 编辑器入口
│   ├── style.css       # 编辑器样式
│   └── script.js       # 编辑器逻辑 (669行)
├── display/
│   ├── index.html      # 展示网站入口
│   ├── style.css       # 展示网站样式
│   └── script.js       # 展示网站逻辑
├── image/              # 图片资源目录 (79个文件)
├── node_modules/       # npm依赖
├── export-ppt.js       # PPT导出脚本
└── package.json        # 项目配置
```

### 6.3 数据模型

```json
{
  "pages": [
    {
      "id": "page_xxx",
      "name": "方案名称",
      "blocks": [
        {
          "id": "block_xxx",
          "label": "区块标题",
          "rows": [
            {
              "height": 200,
              "cells": [
                { "type": "text", "content": "..." },
                { "type": "image", "src": "image/xxx.jpg",
                  "caption": "图片说明", "wallpaper": false }
              ]
            }
          ]
        }
      ]
    }
  ],
  "settings": {
    "bgColor": { "r": 19, "g": 10, "b": 145 },
    "titleFont": { "family": "Microsoft YaHei", "size": 32, "color": "#ffffff" },
    "contentFont": { "family": "Microsoft YaHei", "size": 16, "color": "#ffffff" },
    "noteFont": { "family": "Microsoft YaHei", "size": 14, "color": "#cccccc" }
  }
}
```

---

## 七、设计目标总结

| 目标 | 实现方式 |
|------|---------|
| **沉浸式浏览** | 克莱因蓝背景 + 背景消融 + 全屏展示 |
| **灵活编排** | 页面→区块→行→单元格 四级结构 |
| **所见即所得** | 可视化编辑器，无需写代码 |
| **图纸细节可见** | 卷轴模式支持大图拖拽缩放 |
| **多方案对比** | 多列单元格 + 独立行 |
| **专业排版** | 三级字体系统 + RGB背景色 |
| **数据可迁移** | JSON 导出 + PPT 导出 |
| **零依赖运行** | 纯前端，浏览器直接打开 |

---

*NetDesigning · 建筑设计数字化展陈系统 · 2026*
